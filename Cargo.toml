[package]
name = "eran_codes"
version = "0.1.0"
edition = "2021"

[dependencies]
axum = { version = "0.7.5", features = ["macros"] }
tokio = { version = "1.38.0", features = ["rt-multi-thread", "macros", "tracing"] }
tower-http = { version = "0.5", features = ["fs"] }
