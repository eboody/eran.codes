# jsDelivr Fetch Notes

Attempted to mirror full package files via `data.jsdelivr.com` and `cdn.jsdelivr.net` on 2026-02-28.

Intermittent DNS resolution failures prevented a complete snapshot in this run.
Use `references/reference-map.md` refresh commands and retry when DNS is healthy.
