name: ci

on:
  push:
  pull_request:

jobs:
  stringy-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: stringy checks
        run: scripts/ci/stringy-check.sh
