# mds-datastar-patterns references

The repo no longer mirrors Datastar docs under `/docs/datastar`.

Use these repo-local surfaces first:
- `crates/http/src/views/page.rs`
- `crates/http/src/handlers/pages/`
- `crates/http/src/handlers/demo/`
- `crates/http/src/views/partials/demo/`

For framework semantics, use the installed `datastar` skill or official Datastar docs rather than assuming repo-local mirrors exist.
